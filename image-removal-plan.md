# 问题图撤除方案（**待 R2 验收通过后执行；现在只备不动**）

> Joe 已决定：`4199` / `665` 的问题图撤掉。本方案基于**逐张真看全部 23 张**（不是抽样）——
> 撤图会移动 en 基线，这种决定不能建立在抽样上。

---

## ⚡ 执行清单

| 产品 | 撤 | 撤后剩余图库图 | 理由 |
|---|---|---|---|
| **4199** | `gal3` **+ `inl2`** | 4 → **3** | TP-Link 品牌名 + 机身无 logo + 支架悬空(生成伪影) |
| **665** | `gal3`、`gal5` | 10 → **8**（去重后**7 张不同画面**） | 两张都印着第三方品牌 `Dbilida` |
| **679** | ⚠️ **建议不撤，改字**（见下） | 6 → 6 | 两处都是"改一行字就干净" |

### ⚠️ 与原方案的四处出入（都会影响执行）

**1. `4199` 的 TP-Link 图**在**图库和正文里各有一份，且字节完全相同**（`gal3` = `inl2`）。
→ **只撤正文不撤图库，品牌风险照样挂在页面上。** 两处必须一起撤。
（我上一条报「它只在正文内联、不在 `images[]`」是**错的**——我只查了抽样命中的那一张。）

**2. `665` 的 Dbilida 是 2 张不是 1 张**，而且性质不同：
- `gal5`：把 `Dbilida` 当**自家品牌**做对比图标题 → 撤图能解决
- `gal3`：`Dbilida E248033…` 是**印在线身上的原厂字** → **撤图挡得住图片，挡不住客户收到货看到线身印字**
→ 🔴 **这不是图片问题，是供应链问题：我们卖的这根线本身就是 Dbilida 的。** 需要单独跟 Joe 过。

**3. `679` 建议不撤，改字**——它跟另两个不是一类：
| | 缺陷 | 可挽救? |
|---|---|---|
| `4199_gal3` | TP-Link 品牌 + 生成伪影 | ❌ 无法挽救，只能撤 |
| `665_gal3/gal5` | Dbilida 印在线身上 | ❌ 改不掉 |
| **`679_gal0`** | 红色 USB-A 口下面**标成了 `USB-C`** | ✅ **改一个字母** |
| **`679_gal5`** | 标题 `Water Resi**t**ance`（漏一个 s） | ✅ **改一个字母** |

**撤它们的代价很实**：
- `gal0` 是**首图**（卡片/搜索缩略图）
- `gal5` 是**唯一一张完整全貌图**（车充+盘线+开关+DC 头，构图居中，一眼看懂卖什么）
- 顶替候选 `gal1` **不合适**：六条引线标注，缩略图尺寸下小字糊成噪点，且产品被拆到画面两角、中间大片空场

**旁证`gal0`是标错字而非产品错**：同产品 `gal3` 里 `USB-C Port 20W` / `USB-A Port 18W` **分标正确** → 实物确实是 1C+1A。

**4. `665` 图库里有重复图**：`gal0` 与 `gal1` **字节完全相同**（同一张图放了两遍）。撤完的 8 张里只有 **7 张不同画面**。

---

## 🔴 另外两条，跟撤图无关但更重要

### 1. `665_gal9`：**诚信红线** —— 直埋声明与线材等级不符
图上标题：`Direct Burial - Our Starlink cables can be buried directly`
但线身印的是 `CMX OUTDOOR - CMR` —— **CMR 是竖井级、CMX 是户外受限级，两者都不是直埋（direct burial）等级**。
→ **这是 claims 准确性问题，不是图片美观问题。** 客户照做把线直埋，出问题是我们的责任。
（配图里还根本没有本产品：画的是工人用破碎锤破土，手里那根黑线是电镐的电源线。）

### 2. `665` 的实物是 Dbilida 的线（见上）
撤图只是把它从页面上藏起来。**东西寄到客户手里，线身上印的还是别家品牌。**

---

## 📋 顺带记录（不在本次撤图范围）
- `4199_gal0`：机身中央有**马赛克**（遮 Starlink logo）
- `4199_gal1`：低分辨率素材硬放大到 1920px，边缘块状锯齿
- `4199_gal1 = inl0`、`4199_gal2 = inl1`：图库与正文重复
- `679_gal2`：右边缘车尾有截断文字，放大只能辨出 `XIA` 三字母，**无法确认品牌，故不判风险**；若要稳妥，裁掉右边缘几十像素即可

---

## ⚠️ 执行约束
1. **排在 R2 验收通过之后** —— 撤图/改 alt 都会改 en 输出
2. **单独 commit**，message 明确标注 **「en 基线已移动，请 dev rebase 后重取字节基线」**
3. **绝不夹带进 R2** —— 把内容改动混进重构，会毁掉重构的验收信号

## ❓ 待 Joe / 总调度拍
1. **`679` 改字 vs 撤图**？（撤图要丢首图 + 唯一全貌图；改字只动两个字母）
2. **`665` 卖的是 Dbilida 的线** —— 撤图只治页面，供应链怎么办？
3. **`665_gal9` 的 Direct Burial 声明** —— 与 CMX/CMR 等级不符，是否即刻下掉？（诚信红线，可能不该等 R2）

---

*多语言窗 · 23 张逐张真看（1 个子代理，MD5 校验重复图）· 零改动*
